import styled from 'styled-components';

const Container = styled('div')`
  display: flex;
  flex-wrap: wrap;
  gap: 1rem;
  justify-content: center;
`;

export const PhotoGridStyles = { Container };
