import styled from 'styled-components';

const NavigationContainer = styled('nav')`
  display: flex;
  justify-content: center;
  gap: 3rem;
  .link {
    text-decoration: none;
    color: white;
  }
`;

export const NavigationStyles = { NavigationContainer };
