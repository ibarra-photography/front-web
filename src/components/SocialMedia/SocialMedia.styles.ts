import styled from 'styled-components';

const SocialMediaContainer = styled('div')`
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 3rem;
  flex: 1;
  height: 100%;
`;

export const SocialMediaStyles = { SocialMediaContainer };
